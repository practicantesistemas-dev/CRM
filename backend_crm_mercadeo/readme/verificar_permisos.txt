
-- Verificar permisos
SELECT RP.ROL_ID, R.NOMBRE, RP.PERMISO_ID, P.MODULO, P.ACCION
FROM INTRANET_ROLES_PERMISOS_APP RP
JOIN INTRANET_ROLES_APP R ON R.ID = RP.ROL_ID
JOIN INTRANET_PERMISOS_APP P ON P.ID = RP.PERMISO_ID
WHERE R.SISTEMA = 'CRM_MERCADEO'
ORDER BY R.NOMBRE, P.ID;
